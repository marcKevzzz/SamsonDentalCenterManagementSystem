@page "/Admin/Services"
@model AdminServicesModel
@{
Layout = "AdminSide/_StaffLayout";
var services = Model.Services ?? new List<SamsonDentalCenterManagementSystem.Models.DentalService>();
  }

<!-- Data injected via AdminStore -->

  <div id="page-services" class="page p-4 md:p-6 !pr-0 relative overflow-hidden">

    <div class="flex flex-wrap items-center justify-between gap-3 mb-5 mr-4 md:mr-6">
      <div>
        <h2 class=" text-xl font-bold text-brand">Services</h2>
        <p class="text-[12px] text-muted mt-0.5">Dental services and pricing catalog</p>
      </div>
      <button id="addServiceBtn"
        class="flex items-center gap-1.5 px-4 py-2 rounded-xl text-[12.5px] font-medium text-white bg-primary shadow-md shadow-primary/25 hover:shadow-primary/40 transition-shadow">
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"
          stroke-linecap="round" stroke-linejoin="round">
          <line x1="12" y1="5" x2="12" y2="19" />
          <line x1="5" y1="12" x2="19" y2="12" />
        </svg>
        Add Service
      </button>
    </div>

    <!-- Filter bar -->
    <div class="flex flex-wrap items-center gap-3 mb-6 bg-slate-50/50 p-3 rounded-xl border border-slate-100 mr-4 md:mr-6">
      <div class="relative flex-1 min-w-[240px]">
        <i class="fa-solid fa-magnifying-glass absolute left-3.5 top-1/2 translate-half text-brand/30 text-[11px]"></i>
        <input id="searchInput" type="text" placeholder="Search services by name or description…"
          class="w-full text-[12px] pl-9 pr-4 py-2.5 rounded-xl border border-slate-200 focus:outline-none focus:ring-2 focus:ring-primary/20 transition-all bg-white" />
      </div>
      
      <div class="flex items-center gap-2">
        <span class="text-[10px] font-bold uppercase tracking-wider text-brand/50 ml-2">Category:</span>
        <select id="categoryFilter"
          class="text-[12px] px-4 py-2.5 rounded-xl border border-slate-200 focus:outline-none focus:ring-2 focus:ring-primary/20 bg-white min-w-[140px]">
          <option value="">All Categories</option>
          <option value="General Dentistry">General Dentistry</option>
          <option value="Cosmetic">Cosmetic</option>
          <option value="Specialized">Specialized</option>
        </select>
      </div>

      <div class="flex items-center gap-2">
        <span class="text-[10px] font-bold uppercase tracking-wider text-brand/50 ml-2">Status:</span>
        <select id="statusFilter"
          class="text-[12px] px-4 py-2.5 rounded-xl border border-slate-200 focus:outline-none focus:ring-2 focus:ring-primary/20 bg-white min-w-[120px]">
          <option value="">All Status</option>
          <option value="true">Active</option>
          <option value="false">Inactive</option>
        </select>
      </div>
      
      <span class="ml-auto text-[11px] font-medium text-brand/50 bg-white px-3 py-1.5 rounded-lg border border-slate-100" id="svcCount">
        Initializing...
      </span>
    </div>

    <div id="servicesGrid" class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4  pr-4 md:pr-6">
      <div class="col-span-full py-16 text-center" id="services-loading">
        <div class="w-14 h-14 rounded-2xl bg-blue-50 flex items-center justify-center mx-auto mb-4 text-primary">
          <i class="fa-solid fa-circle-notch fa-spin text-xl"></i>
        </div>
        <p class="text-[13px] text-brand/50">Loading services...</p>
      </div>
    </div>

    <div id="emptyState" class="hidden py-16 text-center pr-4 md:pr-6">
      <div class="text-slate-300 text-4xl mb-3">🦷</div>
      <p class="text-[13px] text-slate-400 font-medium">No services found</p>
    </div>
  </div>

  <div id="svcModal" class="fixed inset-0 z-[99] hidden">
    <div class="absolute inset-0 bg-brand/40 backdrop-blur-md" ></div>
    <div class="absolute inset-0 flex items-center justify-center p-4 pointer-events-none">
      <div id="svcModal-box" class="bg-white w-full max-w-2xl rounded-xl shadow-2xl overflow-hidden pointer-events-auto flex flex-col max-h-[92vh]">
        
        <!-- Modal Header with Banner -->
        <div class="relative bg-brand px-8 py-4 overflow-hidden shrink-0">
          <div class="absolute top-0 right-0 w-64 h-64 bg-blue-500 rounded-full blur-3xl -mr-32 -mt-32"></div>
          <div class="relative z-10 flex items-center justify-between">
            <div>
              <h3 id="modalTitle" class=" font-bold text-2xl text-white tracking-tight">Add Service</h3>
              <p class="text-white/60 text-[12px] mt-1 font-medium">Configure service details and patient information</p>
            </div>
            <button  class="w-10 h-10 rounded-full bg-white/10 text-white hover:bg-white/20 transition-all flex items-center justify-center" id="closeModalBtn">
              <i class="fa-solid fa-xmark"></i>
            </button>
          </div>
        </div>

        <div class="p-4 space-y-4 overflow-y-auto">
          <input type="hidden" id="modalSvcId" />

          <!-- Section: Basic Info -->
          <div class="space-y-4">
            <div class="flex items-center gap-2 mb-2">
              <span class="w-1.5 h-1.5 rounded-full bg-primary"></span>
              <h4 class="text-[11px] font-bold uppercase tracking-wider text-brand/50">Basic Information</h4>
            </div>

            <div class="grid grid-cols-2 gap-4">
              <div class="col-span-2">
                <label class="block text-[11px] font-bold text-brand/60 mb-1.5 ml-1">Service Name *</label>
                <div class="relative">
                  <i class="fa-solid fa-tooth absolute left-4 top-1/2 translate-half text-brand/20 text-xs"></i>
                  <input id="mName" type="text" placeholder="e.g. Teeth Cleaning"
                    class="w-full text-[13px] pl-10 pr-4 py-3 rounded-xl border border-slate-100 bg-slate-50/50 focus:bg-white focus:border-primary focus:ring-4 focus:ring-primary/5 transition-all outline-none" />
                </div>
              </div>
              <div>
                <label class="block text-[11px] font-bold text-brand/60 mb-1.5 ml-1">Category *</label>
                <div class="relative">
                  <i class="fa-solid fa-tags absolute left-4 top-1/2 translate-half text-brand/20 text-xs"></i>
                  <select id="mCategory"
                    class="w-full text-[13px] pl-10 pr-4 py-3 rounded-xl border border-slate-100 bg-slate-50/50 focus:bg-white focus:border-primary focus:ring-4 focus:ring-primary/5 transition-all outline-none appearance-none">
                    <option value="General Dentistry">General Dentistry</option>
                    <option value="Cosmetic">Cosmetic</option>
                    <option value="Specialized">Specialized</option>
                  </select>
                </div>
              </div>
              <div>
                <label class="block text-[11px] font-bold text-brand/60 mb-1.5 ml-1">Status</label>
                <div class="flex items-center gap-4 py-3 px-4 rounded-xl bg-slate-50/50 border border-slate-100">
                  <label class="relative inline-flex items-center cursor-pointer">
                    <input type="checkbox" id="mIsActive" class="sr-only peer" checked>
                    <div class="w-11 h-6 bg-slate-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-primary/20 rounded-full peer peer-checked:after:translate-x-[20px] rtl:peer-checked:after:x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:start-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-primary"></div>
                    <span class="ms-3 text-[12px] font-medium text-brand/70">Active</span>
                  </label>
                </div>
              </div>
              <div>
                <label class="block text-[11px] font-bold text-brand/60 mb-1.5 ml-1">Needs X-Ray</label>
                <div class="flex items-center gap-4 py-3 px-4 rounded-xl bg-slate-50/50 border border-slate-100">
                  <label class="relative inline-flex items-center cursor-pointer">
                    <input type="checkbox" id="mNeedsXray" class="sr-only peer">
                    <div class="w-11 h-6 bg-slate-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-primary/20 rounded-full peer peer-checked:after:translate-x-[20px] rtl:peer-checked:after:x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:start-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-primary"></div>
                    <span class="ms-3 text-[12px] font-medium text-brand/70">Required</span>
                  </label>
                </div>
              </div>
            </div>

            <div class="col-span-2">
              <label class="block text-[11px] font-bold text-brand/60 mb-1.5 ml-1">Tagline *</label>
              <div class="relative">
                <i class="fa-solid fa-quote-left absolute left-4 top-1/2 translate-half text-brand/20 text-xs"></i>
                <input id="mTagline" type="text" placeholder="Short description shown on cards"
                  class="w-full text-[13px] pl-10 pr-4 py-3 rounded-xl border border-slate-100 bg-slate-50/50 focus:bg-white focus:border-primary focus:ring-4 focus:ring-primary/5 transition-all outline-none" />
              </div>
            </div>

            <div class="col-span-2">
              <label class="block text-[11px] font-bold text-brand/60 mb-1.5 ml-1">Summary</label>
              <textarea id="mSummary" rows="3" placeholder="Detailed description for the service page…" 
                class="w-full text-[13px] px-4 py-3 rounded-xl border border-slate-100 bg-slate-50/50 focus:bg-white focus:border-primary focus:ring-4 focus:ring-primary/5 transition-all outline-none resize-none"></textarea>
            </div>
          </div>

          <!-- Section: Images & Media -->
          <div class="space-y-4 pt-4 border-t border-slate-100">
            <div class="flex items-center gap-2 mb-2">
              <span class="w-1.5 h-1.5 rounded-full bg-primary"></span>
              <h4 class="text-[11px] font-bold uppercase tracking-wider text-brand/50">Media & Visuals</h4>
            </div>

            <div class="flex items-center gap-6 p-4 rounded-xl bg-slate-50/50 border border-slate-100">
              <div id="heroPreview" class="w-24 h-24 rounded-xl bg-white border border-slate-100 shadow-sm overflow-hidden flex items-center justify-center shrink-0">
                <i class="fa-solid fa-image text-brand/10 text-3xl"></i>
              </div>
              <div class="flex-1 space-y-2">
                <input type="hidden" id="mHero" />
                <input type="file" id="heroFileInput" accept="image/*" class="hidden" />
                <button type="button" id="heroUploadBtn" 
                  class="px-4 py-2 text-[12px] font-bold text-primary bg-white border border-primary/20 rounded-xl hover:bg-primary hover:text-white transition-all">
                  Choose Hero Image
                </button>
                <p class="text-[10px] text-brand/50">Recommended size: 1920x1080px. Max 10MB.</p>
                <p id="heroUploadStatus" class="text-[10px] text-emerald-600 font-bold hidden"><i class="fa-solid fa-check mr-1"></i> Image ready to upload</p>
              </div>
            </div>
          </div>

          <!-- Section: Pricing & Logistics -->
          <div class="space-y-4 pt-4 border-t border-slate-100">
            <div class="flex items-center gap-2 mb-2">
              <span class="w-1.5 h-1.5 rounded-full bg-primary"></span>
              <h4 class="text-[11px] font-bold uppercase tracking-wider text-brand/50">Pricing & Logistics</h4>
            </div>

            <div class="grid grid-cols-2 gap-4">
              <div>
                <label class="block text-[11px] font-bold text-brand/60 mb-1.5 ml-1">Price *</label>
                <div class="relative">
                  <i class="fa-solid fa-peso-sign absolute left-4 top-1/2 translate-half text-brand/20 text-xs"></i>
                  <input id="mPrice" type="text" placeholder="e.g. 800"
                    class="w-full text-[13px] pl-10 pr-4 py-3 rounded-xl border border-slate-100 bg-slate-50/50 focus:bg-white focus:border-primary focus:ring-4 focus:ring-primary/5 transition-all outline-none" />
                </div>
              </div>
              <div>
                <label class="block text-[11px] font-bold text-brand/60 mb-1.5 ml-1">Duration (Minutes) *</label>
                <div class="relative">
                  <i class="fa-solid fa-hourglass-start absolute left-4 top-1/2 translate-half text-brand/20 text-xs"></i>
                  <input id="mDurationMinutes" type="number" placeholder="e.g. 60"
                    class="w-full text-[13px] pl-10 pr-4 py-3 rounded-xl border border-slate-100 bg-slate-50/50 focus:bg-white focus:border-primary focus:ring-4 focus:ring-primary/5 transition-all outline-none" />
                </div>
              </div>
            </div>

            <div class="grid grid-cols-2 gap-4 mt-4">
              <div>
                <label class="block text-[11px] font-bold text-brand/60 mb-1.5 ml-1">Buffer (Minutes) *</label>
                <div class="relative">
                  <i class="fa-solid fa-shield-halved absolute left-4 top-1/2 translate-half text-brand/20 text-xs"></i>
                  <input id="mBufferMinutes" type="number" placeholder="e.g. 15"
                    class="w-full text-[13px] pl-10 pr-4 py-3 rounded-xl border border-slate-100 bg-slate-50/50 focus:bg-white focus:border-primary focus:ring-4 focus:ring-primary/5 transition-all outline-none" />
                </div>
              </div>
              <div>
                <label class="block text-[11px] font-bold text-brand/60 mb-1.5 ml-1">Recovery</label>
                <div class="relative">
                  <i class="fa-solid fa-bed absolute left-4 top-1/2 translate-half text-brand/20 text-xs"></i>
                  <input id="mRecovery" type="text" placeholder="e.g. None"
                    class="w-full text-[13px] pl-10 pr-4 py-3 rounded-xl border border-slate-100 bg-slate-50/50 focus:bg-white focus:border-primary focus:ring-4 focus:ring-primary/5 transition-all outline-none" />
                </div>
              </div>
            </div>
          </div>

          <!-- Section: Benefits -->
          <div class="space-y-4 pt-4 border-t border-slate-100 ">
            <div class="flex items-center justify-between mb-2">
              <div class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 rounded-full bg-primary"></span>
                <h4 class="text-[11px] font-bold uppercase tracking-wider text-brand/50">Service Benefits</h4>
              </div>
              <button id="addBenefitBtn" type="button" class="text-[11px] font-bold text-primary hover:text-primary-dark flex items-center gap-1">
                <i class="fa-solid fa-plus"></i> Add Benefit
              </button>
            </div>
            <div id="benefitsList" class="grid grid-cols-1 gap-2"></div>
          </div>

          <!-- Section: Process Steps -->
          <div class="space-y-4 pt-4 border-t border-slate-100">
            <div class="flex items-center justify-between mb-2">
              <div class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 rounded-full bg-primary"></span>
                <h4 class="text-[11px] font-bold uppercase tracking-wider text-brand/50">Process Steps</h4>
              </div>
              <button id="addStepBtn" type="button" class="text-[11px] font-bold text-primary hover:text-primary-dark flex items-center gap-1">
                <i class="fa-solid fa-plus"></i> Add Step
              </button>
            </div>
            <div id="stepsList" class="grid grid-cols-1 gap-2"></div>
          </div>

          <!-- Section: FAQ -->
          <div class="space-y-4 pt-4 border-t border-slate-100">
            <div class="flex items-center justify-between mb-2">
              <div class="flex items-center gap-2">
                <span class="w-1.5 h-1.5 rounded-full bg-primary"></span>
                <h4 class="text-[11px] font-bold uppercase tracking-wider text-brand/50">Frequently Asked Questions</h4>
              </div>
              <button id="addFaqBtn" type="button" class="text-[11px] font-bold text-primary hover:text-primary-dark flex items-center gap-1">
                <i class="fa-solid fa-plus"></i> Add FAQ
              </button>
            </div>
            <div id="faqsList" class="grid grid-cols-1 gap-4"></div>
          </div>

          <p id="modalError" class="hidden text-[12px] text-red-500 font-medium bg-red-50 p-3 rounded-xl border border-red-100 text-center"></p>
        </div>

        <!-- Modal Footer -->
        <div class="p-6 border-t border-slate-100 flex items-center justify-end gap-3 bg-slate-50/50 shrink-0">
          <button  
            class="px-6 py-3 text-[13px] font-bold text-brand/60 hover:text-brand/80 transition-colors">
            Discard Changes
          </button>
          <button id="modalSaveBtn" 
            class="px-8 py-3 bg-primary text-white text-[13px] font-bold rounded-xl shadow-lg shadow-primary/25 hover:shadow-primary/40 hover:y-0.5 active:translate-y-0 transition-all">
            Save Service Profile
          </button>
        </div>
      </div>
    </div>
  </div>

  <div id="deleteModal" class="fixed inset-0 z-50 hidden">
    <div class="absolute inset-0 bg-brand/40 backdrop-blur-md" ></div>
    <div class="absolute inset-0 flex items-center justify-center p-4 pointer-events-none">
      <div id="deleteModal-box" class="bg-white w-full max-w-sm rounded-xl shadow-2xl overflow-hidden pointer-events-auto">
        <div class="p-8 text-center">
          <div class="w-16 h-16 rounded-xl bg-red-50 text-red-500 flex items-center justify-center mx-auto mb-6 border border-red-100">
            <i class="fa-solid fa-trash-can text-2xl"></i>
          </div>
          <h3 class="font-bold text-xl text-brand mb-2">Delete Service?</h3>
          <p class="text-[13px] text-brand/50 leading-relaxed mb-8">
            You are about to delete <strong id="deleteSvcName" class="text-brand"></strong>. This action is permanent and cannot be undone.
          </p>
          <div class="flex flex-col gap-2">
            <button id="confirmDeleteBtn"
              class="w-full py-3.5 bg-red-500 text-white text-[13px] font-bold rounded-xl shadow-lg shadow-red-500/20 hover:shadow-red-500/40 hover:y-0.5 active:translate-y-0 transition-all">
              Yes, Delete Service
            </button>
            <button 
              class="w-full py-3.5 text-[13px] font-bold text-brand/60 hover:text-brand/80 transition-colors" id="deleteCloseBtn">
              Cancel, Keep it
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script type="module" src="~/js/AdminSide/adminServices.js"></script>