@page "/Admin/Staff"
@namespace SamsonDentalCenterManagementSystem.Pages.Admin
@model AdminStaffModel
@using SamsonDentalCenterManagementSystem.Services
@{
    
}
@Html.AntiForgeryToken()

<div id="page-staff" class="page p-4 md:p-6">

  <!-- ── Header ──────────────────────────────────────────────────────────── -->
  <div class="flex flex-wrap items-center justify-between gap-3 mb-4">
    <div>
      <h2 class="font-display text-xl font-extrabold text-brand-900">Staff</h2>
      <p class="text-[12px] text-brand-500 mt-0.5" id="staff-summary-text">
        Initializing staff directory...
      </p>
    </div>
    <button onclick="openStaffModal()"
      class="flex items-center gap-1.5 px-4 py-2 rounded-xl text-[12.5px] font-semibold
             text-white bg-primary shadow-md shadow-primary/25 hover:opacity-90 transition-opacity">
      <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor"
        stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
        <line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/>
      </svg>
      Add Staff
    </button>
  </div>

  <!-- ── Filter pills ────────────────────────────────────────────────────── -->
  <div class="flex gap-2 mb-5">
    <button data-filter-btn="all" onclick="filterStaff('all')"
      class="px-3.5 py-1.5 rounded-lg text-[11.5px] font-semibold border border-slate-200 transition-all
             bg-primary text-white shadow-sm">
      All
    </button>
    <button data-filter-btn="doctor" onclick="filterStaff('doctor')"
      class="px-3.5 py-1.5 rounded-lg text-[11.5px] font-semibold border border-slate-200 transition-all
             bg-white text-slate-600 hover:border-slate-300">
      <i class="fa-solid fa-user-doctor mr-1 text-[10px]"></i> Doctors
    </button>
    <button data-filter-btn="receptionist" onclick="filterStaff('receptionist')"
      class="px-3.5 py-1.5 rounded-lg text-[11.5px] font-semibold border border-slate-200 transition-all
             bg-white text-slate-600 hover:border-slate-300">
      <i class="fa-solid fa-headset mr-1 text-[10px]"></i> Receptionists
    </button>
  </div>

  <!-- ── Staff cards ─────────────────────────────────────────────────────── -->
  <div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4" id="staff-container">

      <div class="col-span-full py-16 text-center" id="staff-loading-state">
        <div class="w-14 h-14 rounded-2xl bg-blue-50 flex items-center justify-center
                    mx-auto mb-4 text-primary">
          <i class="fa-solid fa-circle-notch fa-spin text-xl"></i>
        </div>
        <p class="text-[13px] text-brand-400">Loading staff directory...</p>
      </div>

  </div>
</div>

<partial name="Components/StaffModal" />

<script type="module" src="~/js/AdminSide/adminStaff.js"></script>